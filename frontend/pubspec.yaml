name: insightflow
description: InsightFlow Flutter Web frontend.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  csv: ^8.0.0
  excel: ^4.0.6
  file_picker: ^11.0.2
  fl_chart: ^1.2.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
