"""Secure, Excel-only assistant helpers.

This package keeps workbook data local, builds anonymized schema metadata,
parses common natural-language requests into structured commands, validates
those commands, and executes them against the in-memory pandas DataFrame.
"""

